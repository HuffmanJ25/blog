<div class="mdl-grid">
  <div class="mdl-cell mdl-cell--10-col">
    <h1><%= @article.title %></h1>
    <div class="article-body"><%= simple_format @article.body %></div>
    <div class="comment-wrapper">
      <h3>Comments (<%= @article.comments.size %>)</h3>
      <%= render partial: 'articles/comment', collection: @article.comments.reverse %>
      <%= render partial: 'comments/form' %>
    </div>
    <%= link_to "Back to Articles List", articles_path, class: 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent' %>
  </div>
  <div class="mdl-cell mdl-cell--2-col">
    <h2>Tags:</h2>
    <ul class="tag-list">
      <% @article.tags.each do |tag| %>
        <li class="tag-items"><%= link_to tag, tag_path(tag), class: "tags" %></li>
      <% end %>
    </ul>
  </div>
    <% if logged_in? %>
      <%= link_to "Edit the Article", edit_article_path(@article), class: 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent'  %><br />
      <%= link_to "Delete Article", article_path(@article), method: :delete, class: 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent',  data: {confirm: "Are your sure you want to delete this article?"} %><br />
      <%= link_to "Log Out", logout_path, method: :delete, class: 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent'  %>
    <% end %>
</div>
